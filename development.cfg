[buildout]
extends = base.cfg
extensions += mr.developer
find-links += pypi-local/
parts +=
    populate_with_dummies
    env
auto-checkout = *
always-accept-server-certificate = True

[env]
recipe = gocept.recipe.env

[remotes]
getpenelope_push = git@github.com:getpenelope
getpenelope = https://github.com/getpenelope

[sources]
por.models = git ${remote:getpenelope}/por.models.git pushurl=${getpenelope_push}/por.models.git
por.dashboard = git ${remote:getpenelope}/por.dashboard.git pushurl=${getpenelope_push}/por.dashboard.git
por.trac = git ${remote:getpenelope}/por.trac.git pushurl=${getpenelope_push}/por.trac.git
por.gdata = git ${remote:getpenelope}/por.gdata.git pushurl=${getpenelope_push}/por.gdata.git
trac.por = git ${remote:getpenelope}/trac.por.git pushurl=${getpenelope_push}/trac.por.git

[settings]
url = http://localhost:${settings:port}
host = localhost
port = 8081

[populate_with_dummies]
recipe = zc.recipe.egg
eggs =
    por.models
    trac.por

[dashboard-ini]
recipe = collective.recipe.template[genshi]:genshi
input = ${buildout:directory}/templates/development.ini.in
output = ${buildout:directory}/etc/development.ini
