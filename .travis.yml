language: python
python:
  - "2.7"
before_script:
  - psql -c 'create database dashboard;' -U postgres
install:
  - pip install --upgrade setuptools
  - python bootstrap.py -c config/travis.cfg
  - bin/buildout -c config/travis.cfg
script: ./bin/py.test
