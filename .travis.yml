language: python
python:
  - "2.7"
before_script:
  - psql -c 'create database dashboard;' -U postgres
install:
  - easy_install -U setuptools
  - python bootstrap.py -c config/travis.cfg -v 2.2.1
  - bin/buildout -c config/travis.cfg
script: bin/py.test
